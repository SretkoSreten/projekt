{
  "name": "Promo Tech",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run server.js",
    "client": "cd client && npm start"
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  },
  "dependencies": {
    "nodemon": "^3.1.4",
    "threads-graph-api": "^0.1.0"
  }
}
